# WARNING: DO NOT EDIT!
#
# This file was generated by plugin_template, and is managed by it. Please use
# './plugin-template --github pulp_container' to update this file.
#
# For more info visit https://github.com/pulp/plugin_template


---
name: "Container CI Update"
on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    # runs at 3:30 UTC every Sunday
    - cron: '30 3 * * 0'
  workflow_dispatch:

jobs:
  update:
    runs-on: "ubuntu-latest"

    strategy:
      fail-fast: false

    steps:
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          repository: "pulp/plugin_template"
          path: "plugin_template"

      - uses: "actions/setup-python@v5"
        with:
          python-version: "3.11"

      - name: "Install python dependencies"
        run: |
          echo ::group::PYDEPS
          pip install gitpython packaging -r plugin_template/requirements.txt
          echo ::endgroup::

      - name: "Configure Git with pulpbot name and email"
        run: |
          git config --global user.name 'pulpbot'
          git config --global user.email 'pulp-infra@redhat.com'
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_container"
          ref: "main"

      - name: "Run update"
        working-directory: "pulp_container"
        run: |
          ../plugin_template/scripts/update_ci.sh

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_main"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_container"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch main"
          branch: "update-ci/main"
          base: "main"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_container"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_main.outputs.pull-request-number }}"
        if: "steps.create_pr_main.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_container"
          ref: "2.15"

      - name: "Run update"
        working-directory: "pulp_container"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_2_15"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_container"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 2.15"
          branch: "update-ci/2.15"
          base: "2.15"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_container"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_2_15.outputs.pull-request-number }}"
        if: "steps.create_pr_2_15.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_container"
          ref: "2.19"

      - name: "Run update"
        working-directory: "pulp_container"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_2_19"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_container"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 2.19"
          branch: "update-ci/2.19"
          base: "2.19"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_container"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_2_19.outputs.pull-request-number }}"
        if: "steps.create_pr_2_19.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_container"
          ref: "2.20"

      - name: "Run update"
        working-directory: "pulp_container"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_2_20"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_container"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 2.20"
          branch: "update-ci/2.20"
          base: "2.20"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_container"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_2_20.outputs.pull-request-number }}"
        if: "steps.create_pr_2_20.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_container"
          ref: "2.22"

      - name: "Run update"
        working-directory: "pulp_container"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_2_22"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_container"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 2.22"
          branch: "update-ci/2.22"
          base: "2.22"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_container"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_2_22.outputs.pull-request-number }}"
        if: "steps.create_pr_2_22.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_container"
          ref: "2.24"

      - name: "Run update"
        working-directory: "pulp_container"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_2_24"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_container"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 2.24"
          branch: "update-ci/2.24"
          base: "2.24"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_container"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_2_24.outputs.pull-request-number }}"
        if: "steps.create_pr_2_24.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
      - uses: "actions/checkout@v4"
        with:
          fetch-depth: 0
          path: "pulp_container"
          ref: "2.26"

      - name: "Run update"
        working-directory: "pulp_container"
        run: |
          ../plugin_template/scripts/update_ci.sh --release

      - name: "Create Pull Request for CI files"
        uses: "peter-evans/create-pull-request@v6"
        id: "create_pr_2_26"
        with:
          token: "${{ secrets.RELEASE_TOKEN }}"
          path: "pulp_container"
          committer: "pulpbot <pulp-infra@redhat.com>"
          author: "pulpbot <pulp-infra@redhat.com>"
          title: "Update CI files for branch 2.26"
          branch: "update-ci/2.26"
          base: "2.26"
          delete-branch: true
      - name: "Mark PR automerge"
        working-directory: "pulp_container"
        run: |
          gh pr merge --rebase --auto "${{ steps.create_pr_2_26.outputs.pull-request-number }}"
        if: "steps.create_pr_2_26.outputs.pull-request-number"
        env:
          GH_TOKEN: "${{ secrets.RELEASE_TOKEN }}"
        continue-on-error: true
...
